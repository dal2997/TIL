# 260210.md (2026-02-10 TIL)  
*(기준 범위: 02/10 아침 ~ 02/11 현재(새벽) / 요청한 `page.tsx` 작업까지 포함)*

## ✅ Today
- **momonga-fanpage UI/랜딩 개선 흐름 정리**
  - `Hero` 컴포넌트 개선 시도  
    - `quotes` 랜덤 피킹 + parallax(tilt) + 클릭 pop 같은 **가벼운 인터랙션**으로 “휑한 느낌”을 줄이려는 방향 잡음
    - “scroll hint”는 UX상 굳이 필요 없어서 제거 결정

- **다크/라이트 모드 이슈 마무리(핵심 원인 고정)**
  - Tailwind `darkMode: "class"`는 이미 정상이었고, 문제는 **CSS 전역 적용 순서/중복/강제 색상** 쪽으로 수렴
  - `src/app/globals.css` 상단(우선순위 높은 위치)에 필요한 베이스를 넣는 방식으로 **카톡 인앱/특정 환경에서 깨지던 테마 표시가 정상화**
  - 결론: “테마가 안 먹는다”가 아니라, **어딘가에서 white/배경/텍스트가 강제되고 있었던 것**을 정리해서 제거하는 접근이 맞았음

- **공개 프로필 페이지(`src/app/u/[handle]/page.tsx`)까지 작업/리뷰**
  - 공개 페이지 구성 요소들을 한 파일에서 조합하는 형태로 정리:
    - 프로필 조회(`profiles` 느낌의 row) + 컬렉션 리스트(`collections`) + 탭(`all/collecting/collected`) 분기
    - `PublicCollectionGrid`, `OwnerManageButton` (추가로 `PublicStatsSummary` 같은 요약 컴포넌트도 포함 방향)
  - 타입 정리 포인트:
    - `ProfileRow`, `CollectionRow`를 “DB row 기준”으로 명확히 두고
    - UI 탭은 `ViewTab` + `safeTab()`로 **입력값 가드**해서 예외 케이스 줄이는 방식

## 🔜 Tomorrow
- 랜딩(Hero) “휑함” 해결을 **A안부터** 실제로 끝까지 밀어보기
  - (예) 섹션 구성을 더 명확하게: 가치 제안 1줄 + 예시 카드 2~3개 + CTA 1개
- 공개 페이지 UX 다듬기
  - “비로그인/타인 접속”에서 보이는 정보 범위(수집중 포함 공개)와 버튼 노출(수정 불가) 상태를 더 명확히

## 🧩 Issues
- 테마 문제는 “설정”이 아니라 **전역 CSS 우선순위/강제 스타일**이 주범이었음  
  → 앞으로 테마 이슈 생기면: “tailwind 설정”부터가 아니라 **globals/base/reset + 강제 색상 검색**부터 본다.
- `page.tsx`는 컴포넌트 조합이 늘어날수록 복잡해지니  
  → “데이터 fetching/가드(핸들 검증)” vs “렌더링(UI)” 경계가 무너지지 않게 주의.
