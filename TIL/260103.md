# TIL - 260103 (2026-01-03, Sat) 🗓️

## ✅ 오늘 한 것 (내가 “이따가 TIL 써달라”고 한 시점 이후 ~ 밤까지)
- **TIL/기술문서 정리 워크플로우 재정렬**
  - DS_Basics 폴더 구조/스타일(섹션, ## 흐름) 유지하기로 방향 확정
  - “ipynb 중심 정리”를 기본으로 하고, 불필요한 TECH 파일 추가 제안은 최소화하기로 정리

- **수업 노트(Colab) 기반 정리 요청 진행**
  - Survival Analysis 관련 노트 링크를 기준으로
    - “수업 내용 중심 구성 + 코드 위주 리뷰 + 오래 기억하기 위한 포인트” 형태로 정리 방향 확정
  - 동일 제목/동일 데이터로 **엑셀 파일 생성** 요청(재현 가능한 결과물 형태)

- **실습/디버깅 이슈 체크**
  - `centroid>` 프롬프트 상황에서 “왜 출력이 안 보이지?” 같은 실행/출력 흐름 점검
  - 데이터 전처리/결측치 처리 흐름을 계속 따라가며 확인

## 🧩 막힌 것 / 확인한 것
- 출력이 안 보이거나(혹은 셀 실행 결과가 예상과 다르거나) 데이터가 그대로 재현되지 않는 상황이 반복 →  
  “**코드 실행 결과를 기준으로 재현 가능한 형태(엑셀/정리문서)**”로 남기는 게 중요하다고 정리.

## 📌 배운 것 / 메모
- Boolean(True/False)을 **0/1로 바꿔서 쓰고 싶을 때**(모델 입력/저장/가독성 목적):
~~~python
# 1) 가장 흔한 방식
df["col"] = df["col"].astype(int)

# 2) 명시적으로 매핑
df["col"] = df["col"].map({False: 0, True: 1})
~~~

## ➕ 다음에 할 것 (짧게)
- Survival Analysis 노트 정리본을 **“수업 흐름(## 섹션) + 핵심 코드셀 + 체크포인트”** 형태로 DS_Basics 규칙에 맞춰 완성
- 엑셀 파일은 “제목/컬럼/데이터 동일” 기준으로 재생성 + 저장/다운로드 동선 확정

